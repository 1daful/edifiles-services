"use strict";
/*import { IRepository } from "./IRepository";
import { IMediaApi } from "../api/IMediaApi";
import { SoundCloud } from "../api/music/SoundCloud";
export class MediaRepository implements IRepository{
    ajax;
    mediaApi: IMediaApi;

    filters;
    getMedia(filters){
        this.filters = filters;
        this.mediaApi = new SoundCloud(filters);
        this.ajax.get(this.mediaApi);
    }
}*/ 
